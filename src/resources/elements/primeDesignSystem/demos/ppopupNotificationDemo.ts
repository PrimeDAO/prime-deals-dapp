import { autoinject } from "aurelia-framework";

@autoinject
export class PPopupNotificationDemo {
  closed(_id: string): void {
    //const el = document.getElementById(id);
    // el.style.display = "none";
  }
}
